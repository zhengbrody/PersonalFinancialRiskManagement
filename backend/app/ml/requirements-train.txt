# TRAINING-SIDE dependencies ONLY — never installed into the backend Docker
# image (backend/Dockerfile installs requirements.txt + requirements-backend.txt
# and must stay lean for the 916MB t3.micro). Consumers: local training runs
# and .github/workflows/train-regime.yml.
#
# train.py import-guards all of these: without them, training still works
# (built-in config defaults, MLflow logging becomes a no-op). The --cache-dir
# data snapshot uses stdlib pickle, so it needs nothing from this file.
mlflow>=2.14,<4
pyyaml>=6,<7
matplotlib>=3.8,<4
